<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @beer.name %>
</p>


<p>
  <strong>Style:</strong>
  <%= @beer.style %>
</p>

<p>
  <strong>Brewery:</strong>
  <%= @beer.brewery.name %>
</p>

<% if @beer.ratings.empty? %>
 beer has not yet been rated!
<% else %>
 <p>has <%=pluralize(@beer.ratings.count ,'rating')%>, average rating: <%= @beer.average_rating %></p> 
<% end %>

		<%if current_user %>
<%= link_to 'Edit', edit_beer_path(@beer) %> |
<%end %>
<%= link_to 'Back', beers_path %>
